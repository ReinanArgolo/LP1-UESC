#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(void) {

    int a, b, c;
    printf("Digite tres numeros inteiros: \n");
    scanf("%d %d %d", &a, &b, &c);
    printf("%dx^2 + %dx + %d\n", a, b, c);


   
}